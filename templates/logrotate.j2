{{ mongodb_systemlog_path }} {
    daily
    rotate 30
    size 50M
    compress
    dateext
    missingok
    notifempty
    sharedscripts
    postrotate
        /bin/kill -SIGUSR1 `cat {{ mongodb_processmanagement_pidfilepath }} 2> /dev/null` 2> /dev/null || true
    endscript
}
